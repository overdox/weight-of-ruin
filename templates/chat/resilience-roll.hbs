<div class="wor roll-card resilience-roll {{#if passed}}passed{{else}}failed{{/if}}">
  {{!-- Header with Actor Portrait --}}
  <div class="chat-card-header">
    {{#if actor.img}}
    <img src="{{actor.img}}" alt="{{actor.name}}" class="actor-portrait" />
    {{/if}}
    <div class="header-info">
      <h4 class="actor-name">{{actor}}</h4>
      <div class="roll-type">
        <span class="roll-label">
          <i class="fas fa-heartbeat"></i>
          {{localize "WOR.Roll.ResilienceRoll"}}
        </span>
      </div>
    </div>
  </div>

  {{!-- Result Indicator --}}
  <div class="result-indicator {{#if passed}}success{{else}}failure{{/if}}">
    <div class="result-text">
      {{#if passed}}
        <i class="fas fa-shield-alt"></i>
        {{localize "WOR.Roll.ResilienceEndure"}} ( <span class="hit-count">{{hits}}</span> )
      {{else}}
        <i class="fas fa-heart-broken"></i>
        {{localize "WOR.Roll.ResilienceFalter"}} ( <span class="hit-count">{{hits}}</span> )
      {{/if}}
    </div>
    <div class="result-margin">
      {{localize "WOR.Roll.Hits"}} <span class="margin-value {{#if passed}}positive{{else}}negative{{/if}}">{{hits}}</span> vs {{localize "WOR.TT.NetHits"}} <span class="tt-target">{{targetThreshold}}</span>
    </div>
  </div>

  {{!-- Failure Consequences --}}
  {{#if failed}}
  <div class="resilience-result failure">
    <p class="failure-instruction">
      <i class="fas fa-exclamation-triangle"></i>
      {{localize "WOR.Roll.ResilienceFailedConsequences"}}
    </p>
    <div class="consequence-list">
      <div class="consequence">
        <i class="fas fa-minus-circle"></i>
        {{localize "WOR.Health.LostResilience"}}: 1
      </div>
      <div class="consequence">
        <i class="fas fa-heart-crack"></i>
        {{localize "WOR.Health.MaxTraumaReduced"}}: 2
      </div>
    </div>
  </div>
  {{/if}}

  {{!-- Collapsible Details --}}
  <div class="collapsible-section" data-section="resilience-details">
    <div class="collapsible-header collapsed" data-action="toggle-collapse">
      <i class="fas fa-chevron-down collapse-icon"></i>
      <span class="section-title">{{localize "WOR.Roll.Details"}}</span>
    </div>
    <div class="collapsible-content collapsed">
      {{!-- Resilience Stats --}}
      <div class="resilience-stats">
        <div class="stat">
          <span class="stat-label">{{localize "WOR.Health.Resilience"}}:</span>
          <span class="stat-value">{{resilience}} / {{maxResilience}}</span>
        </div>
        <div class="stat">
          <span class="stat-label">{{localize "WOR.Health.Trauma"}}:</span>
          <span class="stat-value trauma-value">{{trauma}} / {{maxTrauma}}</span>
        </div>
      </div>

      {{!-- Dice Display --}}
      <div class="roll-dice">
        {{{diceDisplay}}}
      </div>
    </div>
  </div>

  {{!-- Status Alerts --}}
  {{#if result.isUnconscious}}
  <div class="status-alert unconscious">
    <i class="fas fa-bed"></i>
    {{localize "WOR.Health.Unconscious"}}
  </div>
  {{/if}}

  {{#if result.isDead}}
  <div class="status-alert dead">
    <i class="fas fa-skull"></i>
    {{localize "WOR.Health.Dead"}}
  </div>
  {{/if}}
</div>
